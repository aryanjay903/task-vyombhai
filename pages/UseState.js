import Usestate from "@/task/Usestate";
import { Container } from "reactstrap";
const UseState = () => {
	return (
		<Container className="mt-3">
			<Usestate />
		</Container>
	);
};

export default UseState;
